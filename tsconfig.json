{
  "compilerOptions": {
    "jsx": "react",
    "strict": true, // Enables strict type checking options
    "noImplicitAny": true, // Raise error on expressions and declarations with an implied 'any' type.
    "noUncheckedIndexedAccess": true, // Include 'undefined' in the index signature results
    "baseUrl": ".", // Set a base directory for non-relative module names
    "paths": {
      "app/*": [
        "./packages/app/*"
      ]
    },
    // Consider adding these if you need more control
    // "module": "esnext", // Set depending on your target environment
    // "target": "esnext", // Set based on your execution environment capabilities
  },
  // "extends": "@expo/tsconfig.base"
}